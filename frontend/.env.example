# Copy to .env.local and adjust if your backend runs elsewhere.
# This is the ONLY configuration the frontend needs. It is a public base URL,
# not a secret. Never put API keys / webhook secrets here.

VITE_API_BASE_URL=http://127.0.0.1:8000
